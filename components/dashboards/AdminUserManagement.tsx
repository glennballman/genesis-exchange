import { Card } from '../ui/Card';

const AdminUserManagement = () => {
  return (
    <div className="p-6">
      <h1 className="text-3xl font-bold mb-6 text-white">User Management</h1>
      <Card>
        <p className="text-gray-400">User management functionality will be available in a future version. For now, all users can toggle Admin Mode.</p>
      </Card>
    </div>
  );
};

export default AdminUserManagement;
